# Core inference dependencies (Lambda zip deployment)
numpy>=1.24.0,<2
pandas>=2.0.0,<3
yfinance>=0.2.30
boto3>=1.26.0
scipy>=1.10.0
tqdm>=4.65.0
pyarrow>=12.0.0
requests>=2.28.0
lxml>=4.9.0
lightgbm>=4.0.0
scikit-learn>=1.3.0

# Shared lib (used by inference + training entrypoints)
# Lambda build installs from local path (deploy.sh stages vendor/alpha-engine-lib).
# CI + EC2 install via git URL using ALPHA_ENGINE_LIB_TOKEN for auth
# (see .github/workflows/ci.yml git URL rewrite step).
alpha-engine-lib[arcticdb,flow_doctor] @ git+https://github.com/cipher813/alpha-engine-lib@v0.2.2

# Training dependencies (not needed in Lambda)
arcticdb>=6.11
catboost>=1.2.0
optuna>=3.5.0
pytest>=7.0.0
shap>=0.42.0
matplotlib>=3.7.0

# MLP model — only needed when model_type="mlp" (not used in Lambda)
# torch>=2.0.0
