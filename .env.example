# Alpha Engine Predictor — environment variables
# Copy to .env and fill in values. This file is gitignored.

# ── AWS ──────────────────────────────────────────────────────────────────────
AWS_REGION=us-east-1
S3_BUCKET=alpha-engine-research

# ── Email (training + inference notifications) ───────────────────────────────
EMAIL_SENDER=
EMAIL_RECIPIENTS=
GMAIL_APP_PASSWORD=
